Relocate the nommu RISC-V kernel to chiron's RAM base (0x80000000).

CONFIG_PAGE_OFFSET is the nommu RAM/link base; its default 0x10200000 targets a
machine with RAM at 0x10000000. chiron's DRAM is at 0x80000000, so the kernel
must be linked at 0x80200000 (RAM base + the bbl's 2 MB head room).

Apply from the linux/ source tree:  patch -p1 < this_file
Then: make olddefconfig  (confirm CONFIG_PAGE_OFFSET=0x80200000)

--- a/arch/riscv/Kconfig
+++ b/arch/riscv/Kconfig
@@ -178,7 +178,7 @@ config PAGE_OFFSET
 	hex
 	default 0xC0000000 if 32BIT
-	default 0x10200000 if 64BIT && !MMU
+	default 0x80200000 if 64BIT && !MMU
 	default 0xff60000000000000 if 64BIT
